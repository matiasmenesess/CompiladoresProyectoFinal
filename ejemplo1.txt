#include <stdio.h>

int main() {
    int x = 42;
    int *ptr = &x;
    int *pptr = ptr;

    printf("Valor de x: %d\n", x);
    printf("Valor usando *ptr: %d\n", *ptr);
    printf("Valor usando **pptr: %d\n", *pptr);

    **pptr = 99;
    printf("Nuevo valor de x: %d\n", x);

    return 0;
}