#include <stdio.h>

int suma(int& a, int b) {
    a = a + b;
    return a;
}

int main() {
    int a = 22;
    int b = 3;
    int resultado = suma(a, b);
    printf("La suma es: %d\n", a);
    return 0;
}